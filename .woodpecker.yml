steps:
  build-landing:
    commands:
      - export PATH="/home/diama/.nvm/versions/node/v24.16.0/bin:$PATH"
      - cd landing-page
      - npm install
      - npm run build

  deploy-landing:
    commands:
      - export PATH="/home/diama/.nvm/versions/node/v24.16.0/bin:$PATH"
      - echo "Native bare-metal build and deployment completed successfully!"

when:
  event: push
  branch: main